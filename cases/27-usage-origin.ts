/** Case 27: Usage must remain observable with input and output components. */
import { defineCase } from "./_factory.js";
export default defineCase(27, "27-usage-origin", "Usage pass-through", "usage-origin", "Usage must remain observable with input and output components.", { mode: "live" });
