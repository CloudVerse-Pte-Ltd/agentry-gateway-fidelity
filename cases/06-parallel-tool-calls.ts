/** Case 6: Two independent tool calls should remain parallel where supported. */
import { defineCase } from "./_factory.js";
export default defineCase(6, "06-parallel-tool-calls", "Parallel tool calls", "parallel-tools", "Two independent tool calls should remain parallel where supported.", { mode: "live" });
