/** Case 25: A controlled missing model must not be silently substituted. */
import { defineCase } from "./_factory.js";
export default defineCase(25, "25-model-not-found", "Model-not-found error", "model-not-found", "A controlled missing model must not be silently substituted.", { mode: "fixture" });
