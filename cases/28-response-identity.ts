/** Case 28: Reports whether the response identifies the serving model or provider. */
import { defineCase } from "./_factory.js";
export default defineCase(28, "28-response-identity", "Provider and model identity", "response-identity", "Reports whether the response identifies the serving model or provider.", { mode: "live" });
