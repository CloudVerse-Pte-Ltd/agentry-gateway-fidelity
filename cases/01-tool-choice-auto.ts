/** Case 1: Checks that auto remains permissive and is not forced. */
import { defineCase } from "./_factory.js";
export default defineCase(1, "01-tool-choice-auto", "Tool choice auto", "tool-auto", "Checks that auto remains permissive and is not forced.", { mode: "live" });
