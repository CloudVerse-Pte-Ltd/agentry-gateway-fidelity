/** Case 16: JSON mode must produce parseable JSON. */
import { defineCase } from "./_factory.js";
export default defineCase(16, "16-structured-output", "Structured output", "json", "JSON mode must produce parseable JSON.", { mode: "live" });
