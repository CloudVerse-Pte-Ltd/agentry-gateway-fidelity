/** Case 2: A required tool call must remain required. */
import { defineCase } from "./_factory.js";
export default defineCase(2, "02-tool-choice-required", "Tool choice required", "tool-required", "A required tool call must remain required.", { mode: "live" });
