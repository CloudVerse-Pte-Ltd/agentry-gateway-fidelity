/** Case 22: A controlled policy refusal must remain distinguishable from provider failure. */
import { defineCase } from "./_factory.js";
export default defineCase(22, "22-refusal-shape", "Refusal shape", "refusal-shape", "A controlled policy refusal must remain distinguishable from provider failure.", { mode: "fixture" });
