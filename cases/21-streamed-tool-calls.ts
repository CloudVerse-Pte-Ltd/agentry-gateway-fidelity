/** Case 21: Incremental tool arguments must assemble and terminate correctly. */
import { defineCase } from "./_factory.js";
export default defineCase(21, "21-streamed-tool-calls", "Streamed tool calls", "stream-tools", "Incremental tool arguments must assemble and terminate correctly.", { mode: "live" });
