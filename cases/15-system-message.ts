/** Case 15: A rule present only in the system channel must remain effective. */
import { defineCase } from "./_factory.js";
export default defineCase(15, "15-system-message", "System message placement", "system", "A rule present only in the system channel must remain effective.", { mode: "live" });
