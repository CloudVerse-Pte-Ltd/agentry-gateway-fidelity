/** Case 17: Streaming must contain multiple non-empty deltas. */
import { defineCase } from "./_factory.js";
export default defineCase(17, "17-stream-deltas", "Incremental stream deltas", "stream-deltas", "Streaming must contain multiple non-empty deltas.", { mode: "live" });
