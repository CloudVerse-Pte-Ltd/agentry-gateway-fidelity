/** Case 4: Nested constraints and enums must survive transit. */
import { defineCase } from "./_factory.js";
export default defineCase(4, "04-tool-schema-roundtrip", "Tool schema round-trip", "tool-schema", "Nested constraints and enums must survive transit.", { mode: "live" });
