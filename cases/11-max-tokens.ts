/** Case 11: A tiny output limit must produce observable truncation. */
import { defineCase } from "./_factory.js";
export default defineCase(11, "11-max-tokens", "Maximum tokens", "max-tokens", "A tiny output limit must produce observable truncation.", { mode: "live" });
