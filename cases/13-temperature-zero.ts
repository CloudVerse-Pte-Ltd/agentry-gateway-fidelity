/** Case 13: Repeated temperature-zero requests should remain deterministic. */
import { defineCase } from "./_factory.js";
export default defineCase(13, "13-temperature-zero", "Temperature zero determinism", "determinism", "Repeated temperature-zero requests should remain deterministic.", { mode: "live" });
