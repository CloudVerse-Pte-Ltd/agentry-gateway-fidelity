/** Case 26: Reports baseline divergence as indicative only, never as proof. */
import { defineCase } from "./_factory.js";
export default defineCase(26, "26-model-substitution-advisory", "Model substitution advisory", "model-identity", "Reports baseline divergence as indicative only, never as proof.", { mode: "live", advisory: true });
