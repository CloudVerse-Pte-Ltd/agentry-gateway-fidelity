/** Case 12: Generation must stop at the requested marker. */
import { defineCase } from "./_factory.js";
export default defineCase(12, "12-stop-sequences", "Stop sequences", "stop", "Generation must stop at the requested marker.", { mode: "live" });
