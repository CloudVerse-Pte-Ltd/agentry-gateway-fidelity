/** Case 20: A controlled stream failure must be a typed in-band error. */
import { defineCase } from "./_factory.js";
export default defineCase(20, "20-mid-stream-error", "Mid-stream typed error", "stream-error", "A controlled stream failure must be a typed in-band error.", { mode: "fixture" });
