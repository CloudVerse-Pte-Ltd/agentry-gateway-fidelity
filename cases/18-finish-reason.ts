/** Case 18: The terminal stream event must expose a finish or stop reason. */
import { defineCase } from "./_factory.js";
export default defineCase(18, "18-finish-reason", "Finish reason fidelity", "finish-reason", "The terminal stream event must expose a finish or stop reason.", { mode: "live" });
