/** Case 5: Tool use, tool result, and final completion must remain coherent. */
import { defineCase } from "./_factory.js";
export default defineCase(5, "05-multi-turn-tool-loop", "Multi-turn tool loop", "tool-loop", "Tool use, tool result, and final completion must remain coherent.", { mode: "live" });
