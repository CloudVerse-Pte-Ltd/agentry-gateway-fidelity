/** Case 24: Unknown parameters must be rejected rather than silently dropped. */
import { defineCase } from "./_factory.js";
export default defineCase(24, "24-unknown-parameter", "Unknown parameter handling", "unknown-parameter", "Unknown parameters must be rejected rather than silently dropped.", { mode: "live" });
